% 9.1.1建立符号对象
% 1.建立符号变量和符号常量
% a=sym('a');
% 区分符号变量和数值变量
% pi1=sym('pi');
% sin(pi1)
% sin(pi)
% syms a b c d;
% 2.建立符号表达式
% (1)''
% y='1/sqrt(2*x)';
% f='cos(x^2)-sin(2*x)=0'
% (2)sym函数建立符号表达式
% U=sym('3*x-5*y');
% M=sym('[a,b;c,d]');
% (3)使用已经定义的符号变量组成表达式
syms x y;
V=3*x
